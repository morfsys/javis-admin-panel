import { AllowedCharsDirective } from './allowed-chars.directive';

describe('AllowedCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedCharsDirective();
    expect(directive).toBeTruthy();
  });
});
