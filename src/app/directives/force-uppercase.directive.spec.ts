import { ForceUppercaseDirective } from './force-uppercase.directive';

describe('ForceUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ForceUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
