import { Bs4PopoverDirective } from './bs4-popover.directive';

describe('Bs4PopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new Bs4PopoverDirective();
    expect(directive).toBeTruthy();
  });
});
