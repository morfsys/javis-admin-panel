import { NgSelectizeDirective } from './ng-selectize.directive';

describe('NgSelectizeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgSelectizeDirective();
    expect(directive).toBeTruthy();
  });
});
